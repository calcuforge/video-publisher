# 自动模式默认配置模板（seed）。
# init_platform.py 将其内容合并进 platform_config.yaml 的 default_config 段，
# 作为全新平台的初始默认值。首次发布流程"发布物料数据结构整理"步骤完成后，
# agent 会依据探测到的表单结构完善/覆写这些默认值。
title_format: "{video_name}"
description_format: ""
tags: []
partition: ""
auto_cover: true
publish_script_hint: ""
